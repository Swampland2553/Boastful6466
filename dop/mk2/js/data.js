// Main questions data structure - must be declared before other scripts load
window.questionsData = [];

// Function to add question data
function addQuestion(questionData) {
    window.questionsData.push(questionData);
}